[pytest]
pythonpath = model/src assistant/src
testpaths = model/tests assistant/tests

# Every warning is an error. Silent dtype coercion and pandas deprecations are the
# failure modes most likely to change a metric without changing any code, so they
# should break the build rather than scroll past.
filterwarnings = error

addopts = -ra
